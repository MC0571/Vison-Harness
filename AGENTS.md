# Vision Harness 仓库工作约定

## 开始任务

先确认用户本轮要求、允许修改的范围和停止位置。本文件说明如何工作，不授予实施、合并、发布或外部操作权限。

新会话先读取 [VISION.md](VISION.md) 和 [README.md](README.md)，再按任务读取 [METHOD.md](METHOD.md) 的相关章节。不要每次展开全部历史；遇到跨范围影响或事实冲突时，再补充相关上下文。

工作安排从 [GitHub 规划入口](https://github.com/MC0571/Vison-Harness/issues/2) 和当前 Issue、PR 读取。核对实时状态、有效决定及准确提交，不将聊天摘要、文件存在或旧审查结论当作当前证据。

## 事实和文件

- 目标与方法分别由 VISION.md、METHOD.md 维护；具体 Skill 职责、协作和分发边界见 [ARCHITECTURE.md](ARCHITECTURE.md)。引用已有规则，不在本文件重复全文。
- 工作范围、技术方案、依赖、进展和验收记录放在对应 GitHub 工作中；不新增仓内路线图、任务表或权威进度缓存。
- 需要 Spec 时，按长期领域、能力或契约边界使用无序号的语义目录；先查已有规格，不按每个 Issue 机械创建新文件。
- 普通说明使用通俗中文，保留必要术语、代码标识和文件名。已确认目标、假设、未知和暂缓工作必须分开，不自行补成需求。
- 子目录只有确实存在局部差异时才增加 AGENTS.md。先检查实际宿主的读取规则，不把“文件已创建”当作“规则已加载”。

## 修改与验证

在当前授权范围内连续推进；不得仅凭 Issue 状态、CI 或审查通过扩展权限。未经明确授权，不直接提交 main、不合并、不发布、不改其他项目或使用真实外部账号。

仓内修改使用独立分支和 PR。开始写入前读取完整现状；保留无关内容；利用可用的版本条件检查并发变化；操作结果不明时先查询；写入后回读核对。不要用截断内容或错误响应覆盖有效文件。

构建、测试和检查命令从仓库实际存在的配置、脚本及相关 Issue 获取，先确认可用再执行。没有相应入口时如实报告，不猜测命令，也不将“未执行”写成“通过”。

对代码和确定性工具应用 TDD；对 Skill 或规则行为准备正反场景，并保留实际运行证据。修改 Markdown 指令、AGENTS.md 或配置可能改变 Agent 行为，应按影响选择验证，不能仅按扩展名免检。

文档修改至少检查引用、范围一致性、Markdown 结构和意外文件变更。局部检查不证明真实 Agent 已经能够完成任务；结果必须说明对象、提交和未验证范围。

## 审查和结束

审查按 METHOD.md 第 9、10 节针对 Spec、实现或当前 PR 选择，不机械执行固定轮数。自检、独立审查、验收和操作授权分别表达；没有发现阻断可以是正常结论。

不得为通过当前审查而未经授权放宽相关规则。结束前核对本轮承诺、适用证据和停止边界；当前切片完成不自动关闭长期能力，长期能力未完成也不自动阻塞合格的当前交付。

若当前授权、相关约束和必要条件已经明确，就推进或交付；不为无关远期未知、文档数量或推测性未来功能继续增加前置工作。

## 维护架构与规划时

先对照实际用户任务，说明具体组件承担、独立结果和条件交接；不能只复述 METHOD 或按文档名增加 Skill。保留 Breakdown 名称和有效行为，不机械生成同义的 roadmap-planning 入口。完整职责可先规划，远期保持粗粒度；不要求二十项全实现才能交付当前范围。

Spec/共享规则是独立行为依据，Skill 是工作方法，Eval 验证实际结果。可以共享契约，不按 Skill 数量配套文件，也不通过只改当前实现的指令来重定义验收。涉及已验收内容时保存准确旧候选和影响范围，不将旧结论扩到新契约或新包。

GitHub 维护与仓内修订使用本轮实际授权，不把“由某个 Agent 维护”或任务状态当作长期无限写入权限。共享事实写前复核、写后回读；仓内变更以 PR 交付，未明确获准不自行合并。架构有变化时仅限制受影响实施，不全局停工。
