# Vision Harness 仓库工作约定

本文件是本仓库的基础执行约定。即使没有加载 Vision Harness Plugin、Skill 或其他外部工作流，这些规则也必须成立。Skill 可以提供更具体的方法，但不能扩大授权、改写权威事实、降低验收要求或把自身存在当作完成证据。

## 仓库地图

- [VISION.md](VISION.md)：项目为什么存在、主要用户价值和长期边界。
- [README.md](README.md)：当前产品形态、使用方式和阅读入口。
- [METHOD.md](METHOD.md)：跨任务共用的方法规则、事实边界和完成判断。
- [ARCHITECTURE.md](ARCHITECTURE.md)：Skill 职责、协作和分发边界。
- `specs/`：稳定、可观察的行为契约；不按每个 Issue 机械配套。
- `.agents/skills/`：当前仓库内的 Skill 源；`plugins/vision-harness/` 是装配后的分发候选，不能反向成为规范来源。
- `evals/`：按独立行为依据验证实际 Agent 结果。
- `scripts/`：分发装配及其确定性检查。
- [GitHub 规划入口](https://github.com/MC0571/Vison-Harness/issues/2)：动态工作、关系和交付记录。

把 AGENTS.md 当作地图和仓库级不变量，不当作第二份 METHOD。详细规则留在各自权威文件；只有确实存在局部差异时才在子目录增加 AGENTS.md，并先确认宿主实际会加载该层级。

## 开始任务

先确认用户本轮要求、允许修改的范围和停止位置。本文件说明如何工作，不授予实施、合并、发布或外部操作权限。

非平凡规划或开发先说清一个用户或上游系统可观察的结果、明确不做的事项和少量关键反例。不要用组件清单、文档数量、Issue 数量或“测试全绿”代替交付结果。

新会话先读取 VISION.md 和 README.md，再按当前任务读取 METHOD.md、ARCHITECTURE.md、相关 Spec、Issue 和 PR。只读取足以支撑当前决定的上下文；遇到跨范围影响、证据不足或事实冲突时再扩展，不默认展开全部历史。

开始写入或作出完成判断前，核对当前 Issue / PR、原生关系、准确提交和必要事件记录。聊天摘要、旧评论、文件存在或旧审查结论不能自动证明当前状态。

## 事实与 GitHub 分工

一种事实只维护一个权威来源，不靠多份摘要同步保持一致。至少区分：承诺、当前状态、证据、授权和历史决定。

在 GitHub 中默认按以下职责读取和维护：

- Issue / Milestone 正文：稳定交付合同和导航；合同改变时修订，不滚动维护 SHA、完成百分比或瞬时状态。
- 原生 Issue / Project 字段、parent / dependency、Milestone：当前工作状态和关系。
- PR 正文：本次实际改动、兼容边界、候选范围和固定证据入口。
- 评论 / Review：带时间、范围和来源的实现、验证、验收、授权、暂停、撤销和范围修订事件。
- Git 引用：代码和文档实际上进入了哪个提交。
- Checks / Actions：对应候选实际执行过的自动检查。
- Release 或其他正式分发载体：用户实际上可以取得哪个版本。

不能从 Issue 关闭、Project 状态、reviewer PASS、CI 通过、PR 合并或文件存在，推断实施授权、用户验收、发布完成或长期能力完成；这些事实需要各自的明确依据。

向已有项目应用 Vision Harness 时，先识别并沿用项目已经有效的权威入口、决策方式和审查惯例。VISION.md、ADR、Spec 目录和 GitHub 结构是本项目的推荐形态，不是要求所有消费项目先改造成相同模板。

## 授权、交付与阻塞

在当前授权范围内连续推进；不得仅凭“技术上可以继续”或某个状态变更扩展权限。未经明确授权，不直接提交 main、不合并、不发布、不修改其他项目、不使用真实外部账号或产生额外外部效果。

完整目标保持可见，但当前交付按自己的合同判断。当前版本已经满足其用户结果和放行条件时，可以独立交付；长期 Milestone 尚未完成不自动阻止它。反过来，一次发布、当前切片完成或子 Issue 全关，也不自动关闭长期能力。

遇到缺口时先判断属于哪一类：

- 缺实现；
- 缺正式入口、装配或分发接线；
- 缺授权、现场或外部条件；
- 缺当前候选需要的验证证据。

缺口只阻塞它真实影响的阶段或路径。缺现场授权不能自动变成重构理由；缺实现也不能靠补一段验收说明解决；无关远期未知不能成为当前工作的统一前置。

合并不等于发布，仓内生成物不等于用户已取得版本。发布也不自动证明关联 FR、Milestone 或长期能力已经完成。

## 修改与验证

仓内修改使用独立分支和 PR。共享事实写入前读取完整现状并保留无关内容；在工具能力允许时检查并发变化；结果不明时先查询；写入后回读正文、关系和实际提交。不要用截断内容、旧 SHA 或错误响应覆盖有效文件。

对代码和确定性工具应用 TDD；对 Skill、AGENTS.md、运行参考或规则行为准备正反场景，并保留实际运行证据。Markdown 指令可能改变 Agent 行为，不能仅因扩展名是 `.md` 就按普通说明免检。

本仓当前确定性入口包括：

- `python3 scripts/test_assemble_plugin.py`：装配逻辑的快速回归。
- `python3 scripts/assemble_plugin.py --check`：检查分发候选与权威源一致且装配可重复。
- `git diff --check`：提交前检查空白和补丁格式。

只运行与改动相关的检查；新增或更改其他构建、测试入口时，以仓库实际脚本和配置为准，不猜测命令，也不把“未执行”写成“通过”。AGENTS.md 这类纯仓库规则修改至少检查引用、结构、一致性和意外文件变更；如果规则会改变 Agent 行为，还应补相应行为验证。

证据绑定具体对象、版本、范围和条件。旧候选的 review、测试或真实任务结果不能自动证明新提交、新契约或新分发包；变化不影响原证据时也不机械重跑全部昂贵验证，只重验受影响内容。

测试全绿只证明已经编码的检查通过。结束前仍要回到本轮承诺和相关 Spec，确认所有需要长期保证的行为都有合适证据。

## Code Review Rules

- 审查准确候选和声明范围，先检查是否完整兑现当前承诺，再检查实现质量；不要因为实现看起来合理而静默缩小产品范围。
- 区分自检、独立审查、验收和操作授权。实现者自审不能冒充独立判断，独立审查也不能自动授予合并或发布权限。
- 发现问题时说明影响对象、证据和安全处理方式；区分真正阻断项与非阻断改进，不用风格偏好或推测性未来需求制造完成门。
- 架构、权限、数据、兼容和结果语义发生变化时审查对应长期边界；格式、lint 和可机械检查的规则优先交给脚本或 CI。
- 不机械追求固定 reviewer 数量或审查轮数；准确候选改变后，只重审受影响内容。

## 审查、结束与发布

结束前核对：本轮承诺、实际改动、适用证据、未验证范围和停止边界。结果必须能区分“已实现”“已有内部能力但无正式入口”“已验证”“已发布”“尚待授权或证据”。

若交付事项明确要求合并后回读或验证才能关闭，PR 正文和最终合并提交使用 `Refs #N` 等普通关联，不使用会随合并自动关闭事项的关键词。获准合并后，先核对 main 中的实际候选和必要检查，再在具备关闭授权时显式关闭；合并、验证和事项关闭分别按各自依据判断。其他没有合并后完成条件的事项不受此限制。

不得为了通过当前审查而未经授权放宽规则、降低验收、把未知写成成功，或删除历史证据。若当前授权、必要条件和证据已经足够，就交付或结束，不为文档数量、远期规划或推测性复杂度继续增加前置工作。

## 维护架构与规划时

先从实际用户或上游任务描述交付结果，再说明组件承担、独立结果、依赖和条件交接；不能只复述 METHOD、按目录拆工作或按 Skill 数量生成文件。完整职责可以先规划，远期保持粗粒度，近期才细化到可实施和可验证。

保留 Breakdown 名称和有效行为，不机械生成同义的 roadmap-planning 入口。Spec / 共享规则是独立行为依据，Skill 是完成工作的办法，Eval 验证实际结果；三者可以共享契约，但不要求一一配套，也不能由当前实现反向重定义验收。

真实依赖只表达“缺少另一项结果会阻塞哪一阶段”；parent、优先级和修改冲突不能冒充技术依赖。架构或关键假设未定时只暂停真正受影响的实施，其他调查、准备、验证和独立交付继续。

GitHub 维护与仓内修订使用本轮实际授权，不把“由某个 Agent 维护”、安装了某个 Skill、或任务状态当作长期无限写入权限。