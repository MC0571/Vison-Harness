# Vision Behavior

This runtime reference is generated from the repository's accepted method and behavior sources. It is intentionally self-contained; edit the source documents and rerun the assembly instead of hand-editing this file.

## 输入与适用事实

输入可以是模糊想法、已有愿景、用户反馈或改变前提的新证据。必须区分使用方法的项目与提供方法的 Plugin；方法包的目标、示例和旧决定不自动成为消费项目需求或授权。已有组织级愿景或其他权威入口应被复用，不强制新建 VISION.md。

已有明确答案无需再次询问。只能作有限判断的缺失来源要说明，不能将猜测写成查明事实。已确认目标、候选、假设、未知、暂缓能力和非目标各保留身份；现有实现形式不得静默缩小产品目标。

## 可观察的对话行为

- 只追问会改变当前使用者、问题、结果、主要能力、边界或成功判断的问题，并使用户能理解为什么需要决定。
- 可由材料或工具查明的事实，应在授权和可用工具范围内核对。无法访问时明确缺口，不能把需要事实核验的问题伪装成让用户决定真假。
- 建议须有理由并保留建议身份。等待必要用户决定，不能自问自答、把沉默当同意或按默认选项把关键取舍写成已确认。
- 存在依赖的后续问题不得以尚未得到的回答为前提。收到新回答或前提变更后，只调整受影响的讨论；保留无关的既有共识。
- 允许单个问题、成组问题或开放探索，不固定问卷、题数、轮数，不要求展示决策树或遍历所有远期分支。充分材料下可以没有新增问题。
- 用户暂停时可交付阶段总结，区分已经明确与仍未决定；不得宣称部分讨论已形成完整接受。跨会话保留材料仍服从写入授权。

这些要求规定外部可观察行为，不锁定内部提问算法。某些顺序是约束，例如先得到关键回答再作依赖该回答的决定；报告措辞和合理交互方式不是唯一答案。

## 充分性与结束结果

用于当前规划的愿景应让读者辨认主要使用者、重要问题、预期结果、已知主要能力、产品边界、明确非目标、成功表现与关键未知。不要求固定标题、商业模式、虚构指标、期限或全部技术方案。

剩余未知不影响本轮取舍时，应结束扩展澄清。已有愿景合格就复用；仅一处歧义只处理该处；无新事实时允许无需修改。用户只探索方向时，结果可为选项与未决问题，不能冒充确定的实施范围。

只要求讨论或审查时，结果留在会话；已经授权记录且决定足够时，完成允许的持久化，不追加形式确认。写文件权限不等于产品取舍已被确认。输出须区分共同理解、草案、实际写入和已接受内容，文件存在不证明方向已获接受。

## 修订与副作用

新证据影响愿景时，说明事实、推断和建议，指出受影响范围与仍有效目标。停止某种实现路线不自动取消它承接的需求；需求取消或重要边界改变需要相应决定。不能为让当前代码或验收通过而降低目标。

获准修订只改变允许内容，保留其他有效条款。完整读取、并发处理、结果不明先查询和写后回读沿用 METHOD；无安全更新条件只暂停对应写入，不假称原子更新保证。

授权整理愿景不自动授权建立 Milestone、工程子项、代码、合并或发布。普通共同理解可依现有明确委托持久化，不机械重复索取同一许可。重要历史留在项目现有 Git/PR/Issue，不新建权威会话状态库。
