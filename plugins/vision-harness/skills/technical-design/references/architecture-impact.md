# 架构影响判断

逐项检查：

- 模块或服务责任是否移动、拆分或合并；
- 依赖方向是否反转或形成新环；
- 持久数据、缓存或事件的所有权是否变化；
- 公共协议、schema、版本或兼容窗口是否变化；
- 一致性、事务、并发或恢复模型是否变化；
- 信任边界、权限或敏感数据流是否变化；
- 部署单元、运行环境或运维责任是否变化。

全部为否时，当前架构通常足够，直接给局部路径。任何一项为是也不自动要求全面重构，只设计受影响边界。

例：新增同模块内校验且使用现有错误模型，无架构变化。把认证判断从网关移到各服务则改变信任与责任边界，需要针对性设计。
