# Code 审查

从 diff 开始，追踪被改符号的调用者、数据流和持久化边界。重点检查：与承诺/Spec 一致；状态和所有权；外部输入与权限；错误传播和资源释放；并发、重试、取消；兼容边界；测试是否会在回归时失败。

区分本次新增回归与无关旧问题。确定性格式问题交给工具；风格偏好不是缺陷。复杂度 finding 必须说明哪段抽象/兼容/防御无已确认依据，以及更小替代为何保持正确性。

测试绿色只支持已覆盖入口。缺少核心行为测试可作为 finding；无需把每个分支都要求成独立测试。
