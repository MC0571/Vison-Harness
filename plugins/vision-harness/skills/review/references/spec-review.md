# Spec 审查

固定 Spec 版本和已确认范围，检查：概念与所有权是否一致；前置条件、事件、结果、状态和失败是否可判断；权限、不变量和兼容是否完整；相邻规格是否冲突；建议或技术方案是否被冒充为产品决定。

尝试构造反例：某实现完全符合字面条款，却仍让用户结果失败。若存在，指出缺少的语义，而不是要求特定实现。排版、标题数量和合理实现自由度不构成阻断。

发现格式：位置；触发条件；字面允许的错误行为；违反的确认目标；影响；严重程度；最小语义修正方向。
