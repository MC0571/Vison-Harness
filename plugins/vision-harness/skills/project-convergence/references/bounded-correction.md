# 有界纠偏

## 只诊断

返回具体事实、影响、推荐修改点、仍有效内容和所需授权；不写文件、不改 Issue。

## 授权修改

优先修正负责该事实的单一权威来源，再更新必要导航/引用；完整读取、保护无关内容、检查并发变化并回读。只改受新事实影响的范围，不清空重建。

低风险文档/规划纠偏可在本技能完成。需要改变产品方向、删除确认目标、进行代码重构、合并或发布时分别停止并交接。交接说明准确输入、预期结果和授权缺口，不要求对应 Skill 已安装。

例：两份规则重复同一测试命令且一份过期。保留真实命令来源，在另一处改为引用；不要新增第三份“统一清单”。
