# 构件交付

1. 从 manifest、脚本、CI 或发布配置确认正式构建入口、输出位置和支持环境。
2. 若验证已有包，先记录路径、hash、版本和时间；不要先运行构建覆盖它。
3. 在允许环境运行正式入口，记录命令、退出码和候选。
4. 检查构件包含预期文件、依赖和许可证，不含源码外秘密、缓存、临时文件或维护者材料；核对执行位和路径。
5. 标识最终构件并说明结构检查、安装检查和运行行为分别是否执行。

异常：构建入口会发布到远端而用户只授权本地打包。不要运行；寻找配置中本地构建入口，找不到则报告受阻。
