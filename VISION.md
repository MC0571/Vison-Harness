# Vision Harness

## 愿景

**帮助用户把软件项目的目标说清楚，并让 AI 编程 Agent 在不丢整体目标、不越过授权边界的前提下，可靠、高效地持续交付。**

AI 编程 Agent 已经很擅长完成眼前的代码任务，但长期开发的问题往往不是“这一段代码会不会写”，而是：

- 项目一开始只有一个模糊想法，Agent 是否能帮助用户把它变成足以支持规划的愿景；
- Agent 是否一直知道系统最终要做成什么样，而不是把当前切片误当成完整产品；
- 暂时不做的能力会不会在后续会话中被遗忘；
- 工作之间真正的依赖是什么，哪些可以并行，怎样减少等待和返工；
- 什么时候需要 Spec、架构、ADR、TDD 和专项验证，什么时候不需要；
- Agent 是否会为了“更稳妥”不断增加抽象、兼容路径、检查和流程；
- 测试通过时，是否真的已经完成了本次承诺；
- 新证据出现后，项目能否及时调整甚至停止错误路线，而不是因为已经投入很多就继续做下去。

Vision Harness 要解决的就是这些问题。

它是一套通过 Plugin 和 Skill 融入编程 Agent 工作环境的软件工程方法。它不替用户决定产品方向，也不建立新的项目管理平台；它帮助用户和 Agent 形成、维护并重新理解项目目标，安排工作顺序和并行方式，维护必要的项目约定和审查规则，并在证据和授权足够时继续推进，在不足时准确停下。

## 我们希望改变什么

Vision Harness 希望一个项目从模糊想法开始，也能逐步形成可靠的长期开发上下文。

用户不需要先写出一份完美的 `VISION.md`。Vision Harness 应帮助用户明确：

- 谁会使用这个项目，最重要的问题是什么；
- 最终希望用户或上游系统能够完成什么；
- 已知的主要能力和产品边界是什么；
- 哪些方向明确不属于这个项目；
- 什么表现说明项目正在发挥作用；
- 哪些内容已经确定，哪些仍然是假设或未知。

当这些信息已经足以支持下一步决定时，就应该继续推进，而不是为了“更完整”无限追问。

进入开发后，Vision Harness 继续帮助 Agent 看清：

- 当前工作属于完整目标的哪一部分；
- 本次真正承诺什么，哪些内容只是以后再做；
- 哪些工作存在真实依赖，哪些可以并行，最终如何集成；
- 哪些长期行为、架构或质量保证需要更新；
- 当前选择是否引入了没有依据的复杂度；
- 现有证据究竟支持什么结论；
- 当前授权允许做到哪里。

结果不是“一次把所有功能做完”，而是：

> **先看清整体，再细化眼前；条件不足时停下，条件足够时继续。**

当前实现可以很小，但 Agent 不能丢掉完整目标；完整目标仍有远期部分，也不能成为阻止当前有效交付的理由。

## 成功是什么样

当 Vision Harness 真正发挥作用时，一个新的 Agent、一次新的会话，甚至换到另一个编程环境后，都应该能够从项目已有事实中重新判断：

1. 这个项目最终想构建什么，哪些内容仍然未知；
2. 当前工作为什么存在，它属于整体目标的哪一部分；
3. 本轮被授权做什么，不被授权做什么；
4. 哪些事情已经完成，哪些只是本次交付，哪些长期能力仍未完成；
5. 哪些工作真的互相依赖，哪些可以并行推进；
6. 当前变化需要更新哪些 Spec、架构、项目规则或验证；
7. 现有证据能够支持哪些结论，哪些仍然没有被证明；
8. 现在应该继续、调整、停止，还是请求用户作出决定。

规划还应当让用户看懂最终要交付哪些真实产品组成，而不只是抽象能力名称；后续 Agent 能从当前规划直接进入下一层工作，不需要重新把能力翻译成 Skill、Hook、包、接口或其他交付物。规划完成不以 Issue 或 Milestone 数量为成功依据，而以目标有具体承担方式、阶段结果有证据、下一步可接手为依据。

> **向上能说明为什么值得做，向下能说明具体交付什么，完成后能说明哪些结果真正发生了。**

一个好的结果还意味着：

- 用户不需要反复补充已经确认的背景；
- Agent 不因为远期细节尚未决定就阻塞无关的当前工作；
- Agent 不为了体现“工程完整度”制造无必要的抽象、Issue、文档、检查和审批；
- 当前版本可以在范围明确、真实可用、证据充分时交付，而不会冒充长期目标已经全部完成；
- 错误路线能被证据及时暴露并停止，仍然有效的目标会被正确承接；
- 项目规则和审查约定能被 Agent 找到并正确应用，而不是散落在聊天记录中。

Vision Harness 本身也必须通过 Agent 行为评估证明这些效果，而不是只证明“生成了文档”或“调用了某个 Skill”。

## 它不是什么

Vision Harness 不是项目管理平台、Issue 跟踪系统、路线图数据库、仪表盘、可视化系统或自有工作流引擎，也不会再维护一套与 GitHub 平行的项目状态。

它不会强迫所有项目采用完全相同的目录和文档套餐，不会把每个技术选择都变成 ADR，不会把每个实现步骤都变成 Issue，也不会默认使用“MVP”“先做一个最简单版本”来偷换完整目标。

它同样不会用“安全”“完整”“最佳实践”等理由无限增加流程。需要语义判断的部分由 Agent 按方法执行；能够确定检查的部分应优先复用宿主、GitHub、CI 和现有工具的能力。

它的目标不是让项目拥有更多治理工件，而是让用户和 Agent 更少反复纠偏，在正确的范围、授权和证据条件下持续交付。

## 进一步阅读

- [README.md](README.md)：从使用者角度理解 Vision Harness 会怎样工作。
- [METHOD.md](METHOD.md)：完整的方法规则、事实边界和执行判断。
