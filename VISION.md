# Vision Harness

## 愿景

**让软件项目的 Vision 能够被 AI Coding Agent 有序、高质量、持续收敛地实现。**

AI Coding Agent 已经很擅长完成眼前的代码任务，但长期开发的问题往往不是“这一段代码会不会写”，而是：

- Agent 是否一直知道系统最终要做成什么样；
- 当前工作是否只是完整目标中的一个切片；
- 暂时不做的能力会不会在后续会话中被遗忘；
- 局部功能不断完成后，整体架构和产品目标是否仍然一致；
- 测试通过时，是否真的已经完成了原本承诺的事情。

Vision Harness 要解决的就是这些问题。

它是一套通过 Plugin / Skills 融入 Coding Agent 工作环境的软件工程方法。它不替项目决定产品方向，也不建立新的项目管理平台；它让 Agent 在开始、规划、规格化、设计、编码、验证和后续回顾时，始终从项目已有事实中恢复完整上下文，并按合适的方法继续推进。

## 我们希望改变什么

没有 Vision Harness 时，Agent 很容易围绕当前请求直接开始写代码。例如用户说“给任务执行增加重试”，Agent 可能只增加一个 `retry()`，然后在后续需求里继续给这个局部设计打补丁。

使用 Vision Harness 时，Agent 应先确认这项工作在完整目标中的位置：任务执行最终需要哪些已知能力，本次只做哪些，哪些明确以后再做；如果重试会改变状态模型、接口或长期架构，就先把这些约束说明清楚；然后再从一个小的行为切片开始，用测试驱动实现。

结果不是“一次把所有功能做完”，而是：

> **当前实现可以很小，但 Agent 不能丢掉完整目标。**

这也是 Vision Harness 最核心的原则：

> **Plan the whole. Detail the next.**

先看清整体，再逐步细化眼前工作。对未知保持诚实，不为了“完整”而编造需求；对已经明确但暂不实施的工作，必须留下可追踪的位置。

## 成功是什么样

当 Vision Harness 真正发挥作用时，一个新的 Agent、一个新的 Session，甚至另一个 Coding Environment 进入项目后，都应该能够从仓库和 GitHub 中重新回答：

1. 这个项目最终想构建什么；
2. 当前工作为什么存在，它属于整体目标的哪一部分；
3. 哪些事情已经完成，哪些明确以后做，哪些仍然未知；
4. 当前变化需要更新哪些规格或架构约束；
5. 现在是否已经具备开始实现或结束工作的条件；
6. 这次实现完成后，整个系统是否仍然朝 Vision 收敛。

Vision Harness 本身也必须通过 Agent 行为评估来证明这些效果，而不是只证明“生成了文档”或“调用了某个 Skill”。

## 它不是什么

Vision Harness 不是项目管理平台、Issue Tracker、Roadmap 数据库、Dashboard、可视化系统或自有工作流引擎，也不会再维护一套与 GitHub 平行的项目状态。

它不会强迫所有项目生成相同的文档套餐，不会把每个技术选择都变成正式设计记录，也不会默认使用“MVP”“先做一个最简单版本”来缩小完整目标。

它的目标不是增加流程，而是让 Agent 在长期开发中始终看得见完整目标，在正确的层级做正确的决定，并让每一次局部实现都成为向 Vision 收敛的一步。

## 进一步阅读

- [README.md](README.md)：从使用者角度理解 Vision Harness 会怎样工作。
- [METHOD.md](METHOD.md)：完整的方法规则、事实边界和执行判断。
