# Vision Harness 架构边界

本文件描述方法实现应遵循的职责和依赖边界，不表示相应部件已经全部实现，也不列出交付状态。产品目标见 [VISION.md](VISION.md)，判断原则见 [METHOD.md](METHOD.md)。实际支持范围及证据由相应交付 Issue、PR 和发布记录说明。

## 方法与项目事实分开

Vision Harness 的核心是指导用户和 Agent 作出工程判断。它使用宿主、GitHub、Git 和验证工具，不建立独立的项目管理平台、权限系统、调度服务或工作状态数据库。

GitHub 保存工作范围、关系、授权决定和进展；仓库保存目标、长期行为、结构、规则及实现。可以临时组合这些输入进行判断，但不得将缓存或会话记忆升级为另一份权威项目事实。

## 各部分负责什么

| 部分 | 职责 | 边界 |
| --- | --- | --- |
| 方法规则 | 定义何时澄清、规划、设计、实施、审查、继续或停止 | 不绑定某个宿主的命令，也不承载项目进度 |
| 行为规格 | 精确定义某项方法能力的可观察行为、异常和约束 | 按长期语义组织，不保存工作流状态 |
| Skill | 根据当前任务应用规则，读取必要事实，选择步骤和验证 | 不将整套方法复制成每个 Skill 的独立规则版本 |
| 确定性工具 | 在确有需要时承担可确定检查、可靠读取和获准写入 | 不替用户作产品决定，不暗中维护项目状态 |
| 宿主接入 | 提供发现、加载、工具和权限连接，说明实际保障范围 | 不改变共享方法语义，不将宿主局限包装成产品原则 |
| 测试与行为评估 | 检查工具逻辑及 Agent 的实际决策、操作和结果 | 不以安装、文件生成或自述遵守代替真实行为证据 |

这些是职责划分，不是必须建立六个目录、六个服务或固定数量的 Skill。

## 依赖与规则所有权

用户意图、当前授权和项目事实进入方法判断；Skill 应用相关规则，需要实际操作时使用已获准的工具和宿主能力。验证结果返回相应工作记录，用于下一步判断。

共享规则只维护一处。通用判断不由某个项目模板或单一宿主私有拥有；项目特有约定通过适用的 AGENTS.md 或审查规则补充。新入口应引用相同规则，不复制正文后独立演进。

METHOD.md 保存方法级原则；具体能力进入 Spec 后，精确行为在相应 Spec 中维护。Skill 说明执行方式并引用共同依据，避免方法、Spec、Skill 和宿主配置各自定义相互竞争的标准。

审查规则、审查执行配置和一次审查结果分开。子 Agent 是可选执行方式，不是规范本身，也不能仅凭角色名称证明判断独立。

## 工具能力和权限边界

方法要求“不得继续”，不等于宿主在技术上保证动作不可绕过。需要强制的部分复用实际宿主权限、GitHub 规则和 CI；没有这种能力时明确说明限制，不自建平行工作流引擎。

读取结果必须可辨认来源、对象和适用版本。共享事实写入保留无关内容，使用工具支持的版本条件处理冲突，写后核验；未知结果先查明，再决定重试。仅有写前重读和写后核对，不代表已经取得原子更新保障。

遇到缺少 Milestone、关系或其他写入能力时，报告缺口并保留在相应 GitHub 工作记录中；文字引用不得冒充原生关系。能力缺口不能成为提升权限、改动无关仓库或制造第二份状态库的理由。

## 交付、并行与验证

打包应包含执行所需的共同规则和引用，并从实际交付入口验证可读取性。Skill 加载成功不等于行为正确；本地辅助客户端成功不等于真实 Agent 使用路径成功。

并行分工基于明确输出、共享约定和修改边界。同一共享事实的写入需要协调；没有集成责任和统一候选上的验证，不宣称并行交付完成。

确定性工具与 Agent 行为分别验证。验收证据限定到提交或构件、环境、使用路径和覆盖范围；修改指令和规则可能使行为证据失效。只重验受影响部分，但不能用无关绿色结果掩盖缺失承诺。

## 演进约束

新增抽象、兼容路径或工具应服务已确认需求、可信风险或明确维护需要。正常适配成本不自动否定路线；需要突破产品职责时提出决定，不在实施中静默扩大产品。

宿主选择、具体文件格式、打包方式和工具实现，在相应工作中根据证据决定。只有真正影响长期结构且存在重要取舍的决定才记录 ADR；不为尚未作出的决定创建空记录。
